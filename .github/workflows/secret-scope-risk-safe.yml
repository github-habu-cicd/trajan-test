name: Secret Scope Risk - Safe
on:
  push:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Use secret in trusted trigger
        run: |
          echo "Deploying with secret from push event"
          curl -H "Authorization: Bearer ${{ secrets.API_KEY }}" https://api.example.com/data
