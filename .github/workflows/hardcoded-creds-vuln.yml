name: Hardcoded Container Credentials - Vulnerable
on:
  push:

jobs:
  docker-build:
    runs-on: ubuntu-latest
    steps:
      - name: Login to Docker Hub with hardcoded password
        run: |
          docker login -u myuser -p myHardcodedPassword123
          docker build -t myapp .

      - name: Container step with hardcoded creds
        env:
          DOCKER_PASSWORD: hardcoded_secret_value
          API_TOKEN: abc123def456
        run: echo "Building container"
